#include <cstdio>
#include <vector>

int main(){
  int m, n, hw, p, tp,i;
  scanf("%d %d %d %d", &m, &n, &hw, &p);
  std::vector<std::vector<int>> hp(p);
  int ball[4];
  for (i = 0; i < p; i++){

  }
}