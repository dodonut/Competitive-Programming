#include <iostream>
#include <queue>

int main(){
    int t, val, tot_cost;
    while(scanf("%d",&t),t){
        std::priority_queue<int> q;
        while(t--){
            scanf("%d", &val);
            q.push(val);
        }
        while(tot_cost <= )
    }
}